name: AI repository snapshot

on:
  push:
    branches:
      - main
  schedule:
    - cron: "17 3 1 * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Materialize pinned baoyu-design upstream
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: node vendor/baoyu-design/materialize-upstream.mjs

      - name: Verify bundled baoyu-design upstream
        run: node vendor/baoyu-design/materialize-upstream.mjs --verify-only

      - name: Upload repository snapshot
        uses: actions/upload-artifact@v4
        with:
          name: agent-plugin-marketplace-snapshot
          path: |
            .
            !.git/**
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 90
